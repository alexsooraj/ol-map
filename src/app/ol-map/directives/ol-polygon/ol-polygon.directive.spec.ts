import { OlPolygonDirective } from './ol-polygon.directive';

describe('OlPolygonDirective', () => {
  it('should create an instance', () => {
    const directive = new OlPolygonDirective();
    expect(directive).toBeTruthy();
  });
});
