import { OlLayerDirective } from './ol-layer.directive';

describe('OlLayerDirective', () => {
  it('should create an instance', () => {
    const directive = new OlLayerDirective();
    expect(directive).toBeTruthy();
  });
});
